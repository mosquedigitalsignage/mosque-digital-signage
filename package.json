{
  "name": "mosque-digital-signage",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "build-deploy": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^7.1.0"
  }
}
